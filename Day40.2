class Solution {
    public int getSum(int a, int b) {
        int x = a;
        int y = b;

        while (y != 0) {
            int sum = x ^ y;
            int carry = (x & y) << 1;

            x = sum;
            y = carry;
        }

        return x;
    }
}
